package com.ecomm.ecom.config;

import org.springframework.context.annotation.Configuration;

/**
 * CORS is now configured inside SecurityConfig to work correctly with Spring Security.
 * Keeping this file so the package is not empty.
 */
@Configuration
public class CorsConfig {
}

